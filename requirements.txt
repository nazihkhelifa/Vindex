# Dépendances Python — dépôt Vindex (inférence + métadonnées attention)
#
# vindex_infer_python.py / vindex_infer_ffn_att.py :
#   numpy (obligatoire)
#   tokenizers (recommandé : --prompt et décodage des tops)
#
# build_attn_semantic_meta.py :
#   torch (+ numpy) ; tqdm améliore seulement la barre de progression
#
# Installation : pip install -r requirements.txt

numpy>=1.24
tokenizers>=0.19
torch>=2.1
tqdm>=4.65
